pipeline {
    agent any
    options { 
        timestamps()
        timeout(time: 1, unit: 'HOURS') 
    }
    stages {
        stage('Build & Run Docker Image') {
            steps {
                script{
                    docker.build "test-jenkins:0.1"
                }
            }
        }
    }
}